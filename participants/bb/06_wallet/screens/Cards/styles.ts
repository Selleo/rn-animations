import { StyleSheet } from "react-native";
import { CARDS } from "../../consts";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "white",
  },
  contentContainer: {
    // minHeight: CARDS.length * (240 + 10),
  },
});

export default styles;
