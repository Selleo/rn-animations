import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginTop: 36,
  },
  row: {
    flexDirection: "row",
    justifyContent: "center",
    marginVertical: 4,
  },
});
