import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  background: {
    width: 50,
    height: 50,
    borderRadius: 50,
    backgroundColor: "red",
    position: "absolute",
    bottom: 160,
  }
})

export default styles
